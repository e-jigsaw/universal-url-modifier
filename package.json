{
  "name": "universal-url-modifier",
  "version": "0.0.0",
  "description": "universal url modifier",
  "author": "jigsaw (http://jgs.me)",
  "license": "MIT",
  "devDependencies": {
    "livescript": "^1.4.0",
    "lson-cli": "0.0.5"
  },
  "scripts": {
    "build": "lsc -cbo build src/*.ls && lson -o build src/*.lson"
  }
}
